
import { useAppContext } from '../AppContext';

export default function Header() {
    const { i18n } = useAppContext();

    return (
        <>
            <header>
            
            </header>
        </>
    )
};